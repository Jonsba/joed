#include "children_blocks.h"

// No style at the moment, but later it will have a style for e.g. spacing before and after, block
// margins, ...
Children_Blocks::Children_Blocks() : Multi_Block(nullptr) {}
